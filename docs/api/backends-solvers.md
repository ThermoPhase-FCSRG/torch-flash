# Optional backends and numerical solvers

External backends are optional homogeneous-state references. They are not
silently substituted for native models and are not required by normal tests.

## Optional backends

::: torch_flash.backends

## Numerical solvers

::: torch_flash.solvers
