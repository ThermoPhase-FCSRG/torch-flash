# Equations of state

The constructors on this page create native PyTorch models. Construct them
after calling `configure` so parameter tensors inherit the intended dtype and
device.

::: torch_flash.eos
